services:
  - type: web
    name: aerodoc-pdf-to-md
    env: python
    buildCommand: pip install -e .
    startCommand: uvicorn aerodoc.web.server:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
    healthCheckPath: /
